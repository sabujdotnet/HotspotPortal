# netlify.toml
[build]
  command = "npm run build"
  publish = "build"
  functions = "functions"

# Redirect all non-file requests to index.html for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Environment variables
[build.environment]
  NODE_VERSION = "20"
  NPM_VERSION = "10"

# Cache settings
[build.cache]
  public = ["node_modules"]

# Dev settings
[dev]
  command = "npm start"
  port = 3000
  targetPort = 3000
  publish = "build"

# Context-specific settings
[context.production]
  command = "npm run build:prod"
  publish = "build"

[context.develop]
  command = "npm run build:dev"
  publish = "build"

[context.deploy-preview]
  command = "npm run build"
  publish = "build"

# Custom headers
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    X-XSS-Protection = "1; mode=block"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "geolocation=(), microphone=(), camera=()"

# Cache headers for static assets
[[headers]]
  for = "/*.js"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/*.css"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/*.png"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# API redirects (if backend on different domain)
#[[redirects]]
  #from = "/api/*"
  #to = "https://api.yourserver.com/api/:splat"
  #status = 200
  #force = false
