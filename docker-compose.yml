version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    restart: unless-stopped

  radius:
    image: freeradius/freeradius-server
    ports:
      - "1812:1812/udp"
      - "1813:1813/udp"

  portal:
    image: nginx:alpine
    ports:
      - "80:80"
